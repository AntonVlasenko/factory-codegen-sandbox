# factory-codegen-sandbox

Disposable target repo for the Factory's Phase 11 autonomous codegen → Dagger → PR vertical.
The factory turns a high-level goal into a scoped task, runs Codex to implement it here,
validates with `pytest`, and opens a PR. Safe to change freely.

```sh
pytest -q
```
